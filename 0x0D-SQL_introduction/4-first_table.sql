-- script that create table first_table into database passed by argument
-- i'm not allowed to use SELECT or SHOW statement
CREATE TABLE IF NOT EXISTS first_table(
       id INT,
       name VARCHAR(256)
);
